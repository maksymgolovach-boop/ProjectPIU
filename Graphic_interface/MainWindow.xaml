<Window x:Class="Graphic_interface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Graphic_interface"
        mc:Ignorable="d"
        Title="Orarul personal" Height="450" Width="900" Background="#7461D4"
        WindowStyle="SingleBorderWindow">

 

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- PANELUL LISTEI DE ACTIVITATI -->
        <StackPanel Grid.Column="0"
                    Background="#412DA4">
            <Label Content="Activitati" Foreground="White"
                   HorizontalAlignment="Center"
                   FontFamily="Bahnschrift Light"
                   Margin="0 5"
                   FontSize="20"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" 
                            HorizontalAlignment="Center"
                            Margin="5"
                            Grid.Row="0">
                    <TextBox x:Name="SearchActivity"
                             Width="150"
                             KeyDown="SearchBox_KeyDown"/>
                    <Button Style="{StaticResource CircularPlusButtonStyle}" Margin="5 0 0 0" Click="AddActivityWindow"/>
                </StackPanel>
                
                <!-- LISTA DE ACTIVITATI -->
                <WrapPanel Grid.Row="1" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" MaxHeight="300" MinHeight="250">
                    <ListView x:Name="ActivityListView" Margin="20"
                              VirtualizingPanel.ScrollUnit="Pixel">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#7461D4" CornerRadius="12" Margin="0,10,0,10" Padding="15" Width="150"> 
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0"
                                                   Foreground="#FFFA9E"
                                                   FontFamily="Bahnschrift Light"
                                                   Text="{Binding name}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Margin="0,0,0,5" />

                                        <Rectangle Grid.Row="1" Fill="#40329C" Height="1" Margin="0,0,0,10"/>

                                        <TextBlock Grid.Row="2"
                                                   Foreground="#FFFA9E"
                                                   FontFamily="Bahnschrift Light"
                                                   Text="{Binding description}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap" />   
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </WrapPanel>
            </Grid>
        </StackPanel>

        <!-- REGIUNEA CALENDAR O SA SE DEZVOLTE IN CURAND -->
        <StackPanel Grid.Column="1">
            
        </StackPanel>            
 
         
    </Grid>
</Window>
